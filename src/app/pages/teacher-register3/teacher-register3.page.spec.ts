import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TeacherRegister3Page } from './teacher-register3.page';

describe('TeacherRegister3Page', () => {
  let component: TeacherRegister3Page;
  let fixture: ComponentFixture<TeacherRegister3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TeacherRegister3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
